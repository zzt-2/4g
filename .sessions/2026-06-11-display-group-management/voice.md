# Voice — 接收帧分组管理 Feature

> 用户/导师原话档案，按日期。除零信息推进/应答外都收，不去重。
> 仅标 →产出(可选) 和 ⟶冲突；导师/批注标来源。原话占主体，不加说明。

## 2026-06-21（S008 排查接收页鬼畜）

- "那可以，主要是，分组名得换一个？用中文那个，别弄这种我看不懂的。" → D-emergent-from-frame-def（label 用帧名）
- "我发现好像列表会鬼畜？会时不时出现'暂无字段数据'？还很频繁，不固定，大约一秒一次"
- "在表格有内容（有值）和整个表格都是'暂无字段数据'中间来回变化。按理说不应该出现'暂无字段数据'的，只有没有接收帧定义或者分组啥也没有的时候才会？但肯定是有定义的？你可以从这方面考虑？" → 缺陷 A 根因定位（emergent 分组在 buildPlaceholderRows 眼里=啥也没有）
- "表格不闪了，但是内容闪，就是一会是空的一会有内容，这块问题怎么这么多？是哪里没按规范还是这块搞的就是比较复杂？" → D-buffer-accumulate
- "分组别弄成收到才显示了吧？弄成只要有接收帧就有一个分组？不然它好像也会鬼畜。" → D-emergent-from-frame-def（emergent 立足帧定义不依赖数据）
- "感觉这块代码写的不咋好呢？"

## 2026-06-22（S009 实时测试页布局调整）

- "实时展示改成实时测试，上方的匹配率到字节数给放到表格下面，且字放在一行。表格行间距小一些，改成一半吧？" → S009（三处 UI 调整起因）
- "行间距依然很高？砍到现在三分之二" → 行高目标收紧（28→20px）
- "感觉完全没变化？" → 揭示行高 CSS 两次未生效（根因：父组件 scoped :deep 选择器落在子组件根元素）
- "没用。你先撤回提交吧，改好了一起提交" → reset --soft 撤回 5 提交重做
- "stat我打算让它和开始录制那按钮一行，别单独了。" → stat 并入 bottom-bar 录制行（最终布局）
- "可以了。那六个你靠右，间距大点，不然容易挤开" → S009 续接（stat 移右侧组，gap 改 space-6）
- "太挤了？六个state之间空隙得够大，用来让数字增长不会导致ui被挤走" → S009 续接（每个 stat-item min-width 104px 固定槽位 + gap 24px，数字增长吃槽位留白不推挤相邻项）

## 2026-06-24（S010 星座图刷新间隔接通+点大小）

- "星座图设置里的刷新间隔好像没用,改成 2000ms 没用,再点开还是 100ms。以及,点的大小需要小一些,并且默认间隔至少 2 秒。" → S010（三问题起因：刷新间隔半成品 + 点太大 + 默认间隔太短）
- "它们应该是单独的才对？两个独立" → D001（推翻"统一到顶层 cadence"预判，星座图/波形图各自独立节奏）
- "弃用 C，底栏不再显示单一值" → D001（顶层 refreshCadenceMs 弃用，底栏删假显示）
- "500ms吧？" → D001（表格无入口，固定 500ms 节奏）
- "2吧"（点大小：调默认值+加 pref 字段，选项2） → S010（pointSize 加配置入口，不只调默认值）
- "这些配置有持久化吧？然后，我记得之前想给 rewrite\src\features\display\components\GroupConfigDialog.vue 里的分组加一个导入导出来着，一直没加？" → S011（确认持久化有；GroupConfigDialog 补分组导入导出）
