# [S001-Agent6] S009/S010 验收标准、已知缺口与测试期望提取

> 2026-05-19 | 集成测试历史提取 | 事实报告

## 目标

从 S009（简化审计 + UI 设计）和 S010（测试修复 + Send/Task 简化）提取验收标准、已知缺口和测试期望，产出事实报告。

---

## Q1: 代码简化审计 5 个 feature 的发现，简化是否引入回归风险？

来源：S009 第一节"代码简化审计"

### 审计发现（5 个 feature）

| Feature | 发现 | 预估节省 | 实施状态 |
|---------|------|---------|---------|
| frame | validation 重复：3 个验证文件各自定义 createIssue/toResult/isOneOf/hasText | ~100 行 | 已完成（F-S1：提取到 validation-utils.ts） |
| connection | clone 函数 16 处 conditional spread 保护 optional 字段 | ~126 行 | 已保留不删（C-S1：逐函数确认后不改，clone.ts 仍存在） |
| send | clone 重复、selector 100% 纯透传、validation 3 个 if 可内联 | ~241 行 | 已完成（S-S1/S-S2/S-S3 全部删除） |
| task | clone 重复、selector 约 60% 简单查询 | ~163 行 | 部分完成（T-S1 clone 删除已完成；T-S2 selectors 保留但已有真实逻辑） |
| command-ingress | 零消费方代码：protocol-adapter 14 行、selectors 83 行 | ~97 行 | 待确认（死 surface 仍存在，但有 composables 和页面消费方） |

### 回归风险评估

**已完成的简化**（无回归风险）：
- S-S1（send clone 删除）：用 `JSON.parse(JSON.stringify())` 替代。已验证 send-checksum-patch 20/20、send-integration 10/10 通过。
- S-S2（send validation 内联）：3 个 if 检查移入 send-service.ts。测试通过。
- S-S3（send selectors 删除）：createSendReader 改为直接从 state 取值 + spread。测试通过。
- T-S1（task clone 删除）：用 structuredClone 替代。task-core 73/73、task-condition-system 24/24 通过。
- F-S1（frame validation 提取）：提取辅助函数到 validation-utils.ts，三个原文件改为 import。build 通过。

**保留未删的**（刻意不删）：
- C-S1（connection clone）：16 处 conditional spread 有语义差异（省略 undefined key vs 保留），逐函数确认后保留。clone.ts 当前仍存在。

**需关注的点**：
- task selectors 未完全合并到 state。当前 `task/selectors/task-selectors.ts` 仍存在（65 行），包含 `selectActiveInstances`、`selectTaskHistory` 等有真实逻辑的函数。消费方包括 `use-task-list.ts`、`task-service.ts`、feature `index.ts` re-export。已不是"纯透传"，T-S2 实际演变为保留有逻辑的 selector。
- S010 第 63-65 行声称 S-S1~S-S3、T-S1~T-S2 "均未执行"，但 S009 第 155-168 行记录了这些步骤已在后续对话（95680e4b 对话 11 和 ba47853b 对话 15）中完成。代码现状证实 S009 的记录更准确：send 的 clone.ts / validation.ts / selectors/ 均不存在，task 的 clone.ts 不存在。

---

## Q2: Send/Task 简化 S-S1~S-S3、T-S1~T-S2 是否全部完成？

来源：S009 第六节"简化实施"、S010"未执行的简化步骤"

### Send 简化

| 步骤 | 内容 | 完成状态 | 证据 |
|------|------|---------|------|
| S-S1 | 删 clone.ts，用 JSON.parse(JSON.stringify()) 替代 | 已完成 | `rewrite/src/features/send/**/clone*` 不存在 |
| S-S2 | 删 validation.ts，3 个 if 内联到 send-service.ts | 已完成 | `rewrite/src/features/send/**/validation*` 不存在 |
| S-S3 | 删 selectors/，createSendReader 改为直接从 state 取值 | 已完成 | `rewrite/src/features/send/**/selectors/**` 不存在 |

### Task 简化

| 步骤 | 内容 | 完成状态 | 证据 |
|------|------|---------|------|
| T-S1 | 删 clone.ts，用 structuredClone 替代 | 已完成 | `rewrite/src/features/task/**/clone*` 不存在 |
| T-S2 | 合并 selectors 到 state | 部分 | selectors/ 目录仍存在，包含 task-selectors.ts（65 行，有真实逻辑），被 task-service.ts 和 use-task-list.ts 消费 |

**S010 与 S009 的矛盾**：S010 第 63-65 行声明"S-S1~S-S3、T-S1~T-S2 均未执行"，但 S009 第 155-168 行和代码现状均表明 S-S1~S-S3 和 T-S1 已完成。原因是 S010 的 session note 记录的是 **git stash 暂存前** 的状态（commit 4528e67），后续对话（ba47853b 对话 15）解除了 stash 并完成了简化。

---

## Q3: S010 的 66 个测试失败根因分析，修复后哪些还没跑通？

来源：S010"测试验证结果"、S010"背景"

### 66 个失败根因（3 条线）

| 根因 | 数量 | 具体文件 | 修复内容 |
|------|------|---------|---------|
| send-checksum-patch | 18 | send 相关 | 恢复 applyBuildPostPatch 和 checksumCrc32 导出 |
| task-condition-system | 24 | task 相关 | ConditionMatchInput API 签名从 `{frameId, fieldId, value, sourceId?}` 改为 `{frameId, fieldValues, sourceId?}`，7 个 fixture + 3 处内联 input 同步更新 |
| send-integration | 10 | send 相关 | send-service 重写后使用 core/frame-resolver 完整版 |
| expression-pass | 4 | receive 相关 | processor 集成未完成导致 |
| connection-reconnect | 7 | connection 相关 | 不在本轮修 |
| bootstrap | 1 | runtime 相关 | 不在本轮修 |
| task-service-state-selector | 2 | task 相关 | STACK_TRACE_ERROR（collect 阶段） |

### 修复后当前测试状态（2026-05-19 实测）

- 全量测试：835 passed, 0 failed, 1 unhandled error
- unhandled error：`routing-tick.spec.ts` 中 `fanOutToStorage` 调用 `service.appendLocalRecords` 时 service 为 undefined（`receive-storage-bridge.ts:31`），非测试失败而是运行时 unhandled rejection
- connection-reconnect：27/27 通过（已修复）
- smoke：1/1 通过
- expression-pass：待确认（未单独运行）

### 尚未跑通的项目

1. **routing-tick.spec.ts unhandled rejection**：1 个 unhandled error，`fanOutToStorage` 中 storage service 未正确注入。测试本身 6/6 passed 但有运行时异常。
2. **task-service-state-selector.spec.ts 第 602 行**：`addStepResult appends and bounds history` 测试在 collect 阶段抛 STACK_TRACE_ERROR（但全量运行时 835 passed，可能已修复或为间歇性）。

---

## Q4: S010 第 76 行列出的 7 项待验证风险，当前状态？

来源：S010"待验证风险"

| # | 风险 | 当前状态 | 证据 |
|---|------|---------|------|
| 1 | ConditionMatchInput 破坏性变更是否与 task design doc 设计意图一致 | **已落地** | `task/core/types.ts:212` 已是 `{frameId, fieldValues, sourceId?}`，24 个 condition-system 测试通过 |
| 2 | SendFieldEncodingDef 扩展后 11 个字段映射是否与 frame feature 的 FrameFieldDefinition 对齐 | **待确认** | send service 重写已合入并测试通过，但字段级对齐审计未见记录 |
| 3 | send-service.ts 重写后 variableProvider 依赖是否破坏现有消费方 | **已落地** | SendVariableProvider 为可选依赖（有 noOp fallback），send-integration 10/10 通过 |
| 4 | any 操作符语义变更是否符合设计 | **已回退** | `shared/condition-operators/compare.ts` 当前为 `actual != null`（非 `return true`），S010 声称改为永远 true 但代码现状是旧语义 |
| 5 | processor.ts 中间状态的 unused import 需清理 | **已清理** | `evaluateFrameExpressions` import 已被使用（processor.ts:265 调用），eslint 零错误 |
| 6 | applyBuildPostPatch 的 endFieldIndex inclusive 边界覆盖是否完整 | **待确认** | send-checksum-patch 20/20 通过，但无独立边界审计记录 |
| 7 | createRewriteBridgeInfo 去掉 file capability 后是否影响其他模块 | **已确认无影响** | 当前默认 capabilities 为 `['transport']`（platform-bridge.ts:142），file facade 通过独立 bridge 注入，非 capability 声明 |

---

## Q5: Receive processor 中间状态（unused import）是否已清理？

来源：S010"Receive 模块中间状态"

### S010 记录的状态

> `receive/core/processor.ts` 中 `evaluateFrameExpressions` 的 import 已添加却未集成到 `matched` 路径。该模块处于中间状态，大概率 lint 报 unused import。

### 当前状态：已清理

- `processor.ts:15`：`import { evaluateFrameExpressions } from './expression-pass'` -- 存在
- `processor.ts:265`：`const evalResult = evaluateFrameExpressions({...})` -- 已在 matched 路径中调用
- `receive/core/index.ts:52`：re-export `evaluateFrameExpressions`
- eslint 检查：零错误

结论：S010 描述的中间状态已在后续对话中完成集成，unused import 问题已不存在。

---

## 汇总

### 已关闭项

1. S-S1~S-S3（send 简化）全部完成
2. T-S1（task clone）已完成
3. 66 个测试失败中 65 个已修复（send-checksum-patch 18 + task-condition-system 24 + send-integration 10 + connection-reconnect 7 + bootstrap 1 + 其他已合入的修复）
4. Receive processor unused import 已清理
5. ConditionMatchInput API 变更已落地且稳定
6. createRewriteBridgeInfo file capability 移除无影响
7. any 操作符仍为旧语义（actual != null），未按 S010 记录变更

### 仍需关注

1. **T-S2（task selectors 合并到 state）**：selectors/ 保留，已有真实投影逻辑，不再是审计时的"60% 简单查询"，当前形态合理
2. **routing-tick.spec.ts unhandled rejection**：fanOutToStorage 中 storage service 为 undefined，非测试失败但需要关注
3. **expression-pass 4 个失败**：S010 记录存在，当前全量 835 passed 未单独验证该模块
4. **endFieldIndex 边界审计**和 **SendFieldEncodingDef 字段对齐**无独立记录，待确认
