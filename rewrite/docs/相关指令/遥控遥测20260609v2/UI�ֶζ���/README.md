# UI字段定义

本目录面向上位机软件 UI。

## 目录

- `遥控字段定义/`：UI 输入如何转换成遥控 payload。
- `遥测字段定义/`：遥测 payload 如何截取、转换并显示。

## 基本规则

- 协议字段仍以 `指令与状态/` 为准。
- 本目录只补充 UI 名称、截取 bit、signed/unsigned、控件类型和中文枚举。
- 无法从当前 RTL 注释确定的枚举值，会明确写成“待确认”。
